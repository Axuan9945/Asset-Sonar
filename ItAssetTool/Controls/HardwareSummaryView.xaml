<UserControl
    x:Class="ItAssetTool.Controls.HardwareSummaryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <GridView IsItemClickEnabled="False" SelectionMode="None">
        <GridViewItem Style="{StaticResource GridViewItemStyleNoHover}">
            <Border Style="{StaticResource SummaryCardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8B7;" Style="{StaticResource SummaryCardIconStyle}"/>
                        <TextBlock Text="存储" Style="{StaticResource SummaryCardHeaderTextStyle}"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding StorageCapacity, Mode=OneWay}" Style="{StaticResource SummaryCardTitleStyle}"/>

                    <TextBlock Grid.Row="2" Text="{Binding StorageDetails, Mode=OneWay}" Style="{StaticResource SummaryCardSubtitleStyle}"/>
                </Grid>
            </Border>
        </GridViewItem>

        <GridViewItem Style="{StaticResource GridViewItemStyleNoHover}">
            <Border Style="{StaticResource SummaryCardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE966;" Style="{StaticResource SummaryCardIconStyle}"/>
                        <TextBlock Text="显卡" Style="{StaticResource SummaryCardHeaderTextStyle}"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding GpuInfo, Mode=OneWay}" Style="{StaticResource SummaryCardTitleStyle}" TextWrapping="Wrap" MaxLines="2"/>

                    <TextBlock Grid.Row="2" Text="{Binding GpuDetails, Mode=OneWay}" Style="{StaticResource SummaryCardSubtitleStyle}"/>
                </Grid>
            </Border>
        </GridViewItem>

        <GridViewItem Style="{StaticResource GridViewItemStyleNoHover}">
            <Border Style="{StaticResource SummaryCardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE950;" Style="{StaticResource SummaryCardIconStyle}"/>
                        <TextBlock Text="机带 RAM" Style="{StaticResource SummaryCardHeaderTextStyle}"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding RamCapacity, Mode=OneWay}" Style="{StaticResource SummaryCardTitleStyle}"/>

                    <TextBlock Grid.Row="2" Text="{Binding RamDetails, Mode=OneWay}" Style="{StaticResource SummaryCardSubtitleStyle}"/>
                </Grid>
            </Border>
        </GridViewItem>

        <GridViewItem Style="{StaticResource GridViewItemStyleNoHover}">
            <Border Style="{StaticResource SummaryCardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE958;" Style="{StaticResource SummaryCardIconStyle}"/>
                        <TextBlock Text="处理器" Style="{StaticResource SummaryCardHeaderTextStyle}"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding CpuModel, Mode=OneWay}" Style="{StaticResource SummaryCardTitleStyle}" MaxLines="3" TextWrapping="Wrap"/>

                    <TextBlock Grid.Row="2" Text="{Binding CpuDetails, Mode=OneWay}" Style="{StaticResource SummaryCardSubtitleStyle}"/>
                </Grid>
            </Border>
        </GridViewItem>
    </GridView>
</UserControl>